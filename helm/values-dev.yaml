sync:
  image:
    tag: dev

reindex:
  image:
    tag: dev
  minFreeDiskGiB: 5   # PVC is 10 GiB; prod default (20) would always fail here

typesense:
  resources:
    requests:
      cpu: 100m
      memory: 512Mi
    limits:
      cpu: 500m
      memory: 4Gi

cleanup:
  image:
    tag: dev

contentExtract:
  enabled: true
  image:
    tag: dev
  env:
    CONTENT_EXTRACT_CONCURRENCY: "3"
  resources:
    requests:
      cpu: 100m
      memory: 512Mi
    limits:
      cpu: 500m
      memory: 2Gi

searchKeyInit:
  image:
    tag: dev
